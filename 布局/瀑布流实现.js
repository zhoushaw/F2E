/**
 * 瀑布流生成
 *
 * 首先获取所有需要加载的图片
 * 将创建div，将图片内容放好
 * 如何一个变量等待加入到页面
 * 设定一个值为图片盒子的大小，获取当前浏览器屏幕大小，
 * 对其进行除运算计算列数
 * 生成一个数组，初始化跟列数相同大小的列数，并且设置值为0
 * 拿到将其加入页面的父节点，对已经创建好了的图片进行遍历
 * 当遍历的i小于列数时，直接将其添加进content中
 *
 */